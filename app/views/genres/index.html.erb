<h1>Book Genres</h1>

<% @genres.in_groups_of(3, false).each do |group| %>
  <div class='row'>
    <% group.each do |genre| %>
      <div class='col-md-4'>
        <h2><%= link_to genre.name, genre_path(genre) %></h2>
      </div>
    <% end %>
  </div>
<% end %>