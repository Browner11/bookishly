<h1>Books</h1>
<%= paginate @books%>
<% @books.in_groups_of(3, false).each do |group| %>
  <div class='row'>
    <% group.each do |book| %>
      <div class='col-md-4'>
        <h4><%= book.name %></h4>
      </div>
      <% end %>
  </div>
<% end %>
<%= paginate @books  %>